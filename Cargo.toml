[package]
name = "gpui-shell"
version = "0.1.0"
edition = "2024"

[dependencies]
gpui = { version = "0.2.2", default-features = false, features = ["wayland"], git = "https://github.com/zed-industries/zed.git" }
gpui_platform = {
		version = "0.1.0",
    features = ["wayland"],
    git = "https://github.com/zed-industries/zed.git",
}
